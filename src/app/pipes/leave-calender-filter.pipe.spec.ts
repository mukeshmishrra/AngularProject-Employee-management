import { LeaveCalenderFilterPipe } from './leave-calender-filter.pipe';

describe('LeaveCalenderFilterPipe', () => {
  it('create an instance', () => {
    const pipe = new LeaveCalenderFilterPipe();
    expect(pipe).toBeTruthy();
  });
});
